#include <Mongoose.hpp>

int main (int argc, char *argv[])
{
    printf ("*************************************************************\n") ;

    printf ("Mongoose is a graph partitioning library.\n"
            "Currently, Mongoose only supports edge partitioning " 
            "but in the future a vertex separator extension will "
            "be added.\n\n") ;

    printf ("As we are looking for a vertex partitioning or an "
            "element partitionning, we will not try Moogoose for "
            "the moment") ;

    printf ("*************************************************************\n") ;

    return (0) ;  // Successfull exit
}